# 작업 결과 정리 — 들렀다감 v0.1

`deulleotdagam-handoff/`의 기획·설계 문서(CLAUDE.md, docs/01~06, 99)와 기준 프로토타입(`pixel-3trees.html`)을 바탕으로
**마일스톤 1~7 전체**를 구현하고, 단계마다 단위/통합/브라우저 테스트로 확인했습니다. (작업일: 2026-09-23)

## 1. 한눈에 보기

| 항목 | 내용 |
|---|---|
| 구조 | npm workspaces 모노레포: `@deulleotdagam/shared`, `@deulleotdagam/worker`, `@deulleotdagam/web` |
| 언어/도구 | TypeScript 전체, Vite 8, Wrangler 4, Vitest 4 |
| 백엔드 | Cloudflare Worker `deulleotdagam-api` + Durable Object `RoomDO`(SQLite, 방 1개 = 객체 1개) + D1 `deulleotdagam-index`(색인) |
| 실시간 | 처음부터 웹소켓 (Hibernation API, ping은 런타임 자동 응답이라 객체를 깨우지 않음) |
| 프론트 | 프레임워크 없는 TS 모듈 (프로토타입 DOM + Pointer Events 코드 이식) |
| 테스트 | 단위 27개 + API/웹소켓 통합 23개 + 정책(시간 경과) 7개 = **57개 전부 통과**, 브라우저 수동 시나리오 20여 개 확인 |
| 번들 크기 | 프론트 JS 28KB(gzip), Worker 14KB(gzip) |

## 2. 마일스톤별 구현 내용

### M1. 프로젝트 골격
- 이름 규칙 표 그대로: 패키지 `@deulleotdagam/*`, Worker `deulleotdagam-api`, D1 `deulleotdagam-index`, DO `RoomDO`, `<title>` "들렀다감 · 겨울 트리 꾸미기"
- `npm run dev` 한 번으로 D1 마이그레이션 → Worker(8787) + Vite(5173, `/api`·웹소켓 프록시)
- npm 11의 peer 의존성 해석 버그(`edgesOut` 오류) 때문에 `.npmrc`에 `legacy-peer-deps=true`

### M2. 공용 모듈 (`packages/shared`)
- `theme.ts`: 픽셀 테마 기하 값(나무 윤곽 ROWS, 4개 층 구역, 꼭대기 슬롯, 나무 3그루, 조명 띠 3개, 장식/조명 목록, 기본 조명)을 **테마 설정 객체**로 분리 → 테마 추가 시 객체 하나만 더 만들면 됨
- `slots.ts`: 04 문서의 층별 구역 + 최소 칸 보장 알고리즘 (mulberry32 시드)
- `rules.ts`: `accepts`(별↔꼭대기), `canDelete`(본인/방장), 메모 40자·방 이름 20자·이름 10자 검증, 금칙어 필터(공백·특수문자·숫자 끼워 넣기 대응, 흔한 표현 오탐 방지)
- `ids.ts`: roomId(128bit), 참여 코드(8자, 혼동 문자 제외), **방장 키(20자 ≈ 99bit, 프로토타입 8자보다 훨씬 김)**, 게스트 토큰, SHA-256 해시
- `seasons.ts`, `protocol.ts`(웹소켓/HTTP 메시지 타입), `names.ts`(자동 이름)
- `scripts/extract-tree-rows.mjs`: `tree.svg`의 `<rect>`에서 줄별 윤곽을 뽑는 빌드 스크립트. 테스트에서 코드의 표와 일치하는지 확인

### M3. 방 생성/입장 API
| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/season` | 현재/지난 시즌 정보 |
| POST | `/api/rooms` | 생성 제한 확인 → 색인에 참여 코드 확보 → RoomDO 초기화(슬롯 1회 생성·저장) → `ownerKey`는 이때 한 번만 응답 |
| GET | `/api/join/:code` | 참여 코드로 입장 (소문자/하이픈 허용) |
| POST | `/api/owner/resume` | 방장 키로 재입장 (본문으로만 받음, URL에 넣지 않음) |
| POST | `/api/rooms/:id/delete` | 방장 키 + 방 이름 확인 후 영구 삭제 |
| GET | `/api/rooms/random-public` | 공개방 랜덤 (`rand` 인덱스 방식, `ORDER BY random()` 미사용) |
| GET | `/api/archive` | 지난 시즌 목록 (완성 우선, 10개 미만이면 미완성 포함) |
| GET | `/api/rooms/:id/snapshot` | 지난 시즌 공개방 읽기 전용 스냅샷 |
| GET | `/api/rooms/:id/ws` | 웹소켓 (없는 roomId로 DO가 생기지 않게 색인에서 먼저 확인) |
| GET/POST | `/api/admin/*` | 비활성 방 목록, 복구/비활성/삭제 (`ADMIN_TOKEN`) |

### M4. 꾸미기 화면 (`packages/web`)
- 프로토타입 UI를 모듈로 분리: 테마 에셋, 장면 렌더러, 화면(메인/방/지난 시즌/안내/개인정보), 대화상자, 저장소, 공유
- 드래그(8px 이상 이동, 가장 가까운 허용 슬롯 스냅) / 탭 선택 / 키보드 Enter·Space, 모바일 가로 목록의 스와이프·드래그 구분
- localStorage 저장 → **웹소켓 메시지**로 교체. 서버 이벤트를 받은 뒤 반영(낙관적 반영 없음, 전송 중 슬롯은 흐리게)
- 전체 다시 그리기 대신 **바뀐 슬롯만 갱신**
- 프로토타입의 테스트용 "프로토타입 설정" 띠는 제거하고 하단 방 메뉴(이름 바꾸기, 반짝임, 사진, 신고, 방 삭제)로 교체
- 새로 만든 화면: 메인(방 만들기/코드 입장/랜덤 공개방/방장 ID 입장/지난 시즌/최근 들른 방), 방 삭제 확인, 이름 바꾸기, 신고, 사진 공유, 지난 시즌 갤러리, 없는 방·비활성·삭제·사용량 초과 안내

### M5. 실시간 동기화
- 방 안의 모든 읽기/쓰기는 RoomDO 안에서 처리. D1은 생성·공개 여부·완성 여부·삭제·비활성·시즌 종료 때만 갱신
- 모든 변경에 `rev`를 붙여 방송하고, 클라이언트는 번호가 건너뛰면 다시 연결해 스냅샷을 새로 받음
- 끊기면 지수 백오프(최대 15초)로 재연결하고, 여러 번 실패하면 방이 삭제/비활성됐는지 확인
- 소켓별 권한(guestId, isOwner)은 `serializeAttachment`, 메시지 빈도 제한(토큰 버킷), 메시지 크기 제한

### M6. 정책
- **자동 삭제**: 생성 시 알람을 +30일로 설정 → 장식 5개 미만이면 색인 삭제 → 알람 해제 → `deleteAll`
- **방장 삭제**: 방 이름 입력 확인 → 접속자에게 "방장이 방을 삭제했어요" 후 연결 종료 → 색인 먼저 삭제 → 저장소 삭제
- **생성 제한**: IP를 비밀 솔트로 해시해 하루 5개. 지난 기록은 매일 00:00 UTC Cron으로 정리
- **금칙어**: 메모·방 이름·참여자 이름에 적용 (서버에서 최종 판단)
- **신고**: 방/장식 신고 → 서로 다른 신고자(IP 해시 기준) 3명이면 비활성 전환 → 접속자 연결 종료, 입장 불가 → 관리자 API로 복구 시 신고 누적 초기화
- **개인정보**: IP·방장 키·게스트 토큰은 모두 해시로만 저장

### M7. 부가 기능
- 공개방 랜덤 입장, 지난 시즌 둘러보기(썸네일 갤러리 + 읽기 전용 보기 + 빈 상태 문구), 완성 결과 이미지(PNG) 저장/공유(Web Share), 메모 포함 토글(기본 꺼짐)

## 3. 미결정 사항 처리 (`docs/06-open-questions.md`)

모두 문서의 **임시안**으로 구현했고, 코드에 `TODO(open-question #번호)`를 남겼습니다. 확정되면 해당 위치만 바꾸면 됩니다.

| # | 항목 | 적용한 임시안 | 위치 |
|---|---|---|---|
| 3 | 지난 시즌 편입 시점 | 시즌 종료 후에만 | `worker/src/index.ts` archive |
| 4 | 자동 삭제 5개 기준 | 방 전체 기준 | `worker/src/room.ts` |
| 5 | 나무 수 | 3그루 고정 | `shared/src/theme.ts` |
| 6 | 참여자 이름 | 자동 이름("눈사람 12") + 변경 가능 | `shared/src/names.ts` |
| 7 | 신고 임계치 / 관리자 | 3명, 관리자 API로 처리 | `wrangler.jsonc`, `index.ts` |
| 8 | 금칙어 목록 | 코드 기본 목록 + `BANNED_WORDS` 환경변수 | `shared/src/rules.ts` |
| 9 | 생성 제한 | IP 해시당 하루 5개 | `wrangler.jsonc` |
| 10 | 시즌 기간 | 12/1~12/31 KST | `shared/src/seasons.ts` |
| 11 | 공유 형태 | 클라이언트 PNG + 메모 토글 | `web/src/share.ts` |
| 12 | 반짝임 끄기 | 사용자 설정으로 남김 | `web/src/storage.ts` |
| 13 | 개인정보처리방침 | 페이지 자리만 | `web/src/screens/privacy.ts` |
| 14 | 키 분실 방 정리 | 시즌 후에도 보관 (별도 정리 없음) | — |

### 문서에 없어서 제가 정한 것 (확인 부탁드려요)
1. **지난 시즌 둘러보기에는 공개방만 노출**합니다. 비공개방은 참여 코드를 아는 사람끼리의 공간이라 모르는 사람에게 보여주지 않는 쪽이 안전하다고 판단했습니다.
2. **시즌 시작(12/1) 전에도 방을 만들 수 있게** 했습니다. 막으면 지금(9월) 서비스를 쓸 수 없어서입니다. 메인에 "미리 꾸며도 돼요"라고 표시합니다.
3. 신고 수는 **방 전체에서 서로 다른 IP 해시 수**로 셉니다. 브라우저 저장소를 지워 게스트를 새로 만들어도 혼자서는 1명으로 셉니다.
4. 방장은 방장 키로 들어오면 **항상 같은 게스트 ID**를 쓰도록 해서, 다른 기기에서 들어와도 자기 장식이 "내 장식"으로 보입니다.
5. 방장 키는 편의를 위해 **만든 브라우저의 localStorage에도 보관**합니다(새로고침해도 방장 유지). 다른 기기에서는 키 입력이 필요합니다.
6. 시즌이 끝난 방은 참여 코드로 들어가도 **읽기 전용**으로 열립니다(이 경우 게스트를 저장하지 않아 쓰기 한도를 아낌).

## 4. 테스트와 확인 결과

### 자동 테스트 (57개 통과)
- **단위(27)**: 슬롯 생성 3,000개 시드로 구역별 최소/최대 위반 0건, 몸통 10~14칸 고른 분포, 겹침 없음, 윤곽 안, 결정적 결과, `tree.svg` 윤곽 일치 / 권한·검증·금칙어·식별자·시즌
- **API/웹소켓 통합(23)**: 방 생성·이름 검증, 코드/방장 키 입장, 게스트 토큰 재입장, 방장 인식, 장식 규칙(별↔꼭대기, 없는 장식, 메모 길이·금칙어), 실시간 방송과 rev, **같은 슬롯 동시 요청 시 한쪽만 성공**, 삭제 권한 3종, 완성 기록, 빈도 제한, 조명·공개 전환·이름 바꾸기 권한, 방 삭제(웹소켓/HTTP), 신고 누적 → 비활성 → 관리자 복구, 진행 중 시즌의 지난 시즌 목록
- **정책(7)**: 같은 IP 6번째 생성 차단, 자동 삭제(장식 4개 방 삭제 / 5개 방 유지), 시즌 종료 후 방 생성 불가·목록 순서·비공개 제외·읽기 전용 스냅샷·장식 달기 거부
- Cron(지난 생성 기록 정리) 수동 실행 확인, 세 패키지 타입 검사, 프로덕션 빌드, Worker 배포 dry-run 통과

### 브라우저에서 직접 확인한 것 (Chrome, 데스크톱 + 390px 모바일 폭)
방 만들기 → 방장 ID 안내 팝업(바깥 클릭/ESC로 안 닫힘) → 드래그로 장식 달기 + 한글 메모 Enter 확정 → 마우스 올리면 메모 말풍선 → 다른 참여자가 단 장식이 실시간으로 나타남 → 방장 권한으로 떼기 → 별 탭 선택 시 꼭대기만 강조 → 조명 바꾸기 → 새로고침 후 상태 유지 → 사진 만들기(메모 포함 시 목록 추가) → 참여자 화면 권한(방장 버튼 숨김, 신고만 보임) → 이름 바꾸기 → 공개 전환 → 방 삭제(이름 입력 전 버튼 비활성, 삭제 시 참여자 연결 종료) → 삭제된 방 주소 안내 → 방장 ID로 재입장 → 모바일 레이아웃·탭 선택 → 서버 재시작 시 "연결이 끊겼어요" 표시 후 자동 재연결 → 지난 시즌 갤러리·읽기 전용 보기. 콘솔 오류 없음.

### 테스트 중 발견해서 고친 버그
- 신고 중복 방지가 동작하지 않음(`INSERT OR IGNORE`의 `rowsWritten`에 의존) → 존재 확인 후 삽입으로 수정
- 방 화면이 화면 높이를 다 쓰지 못함(`#app` 높이 누락)
- 지난 시즌 빈 상태의 `.empty` 클래스가 슬롯의 `.slot.empty`와 겹쳐 빈 자리가 검은 타원으로 보임 → `.empty-state`로 이름 변경
- 메인 입력칸 옆 버튼이 줄바꿈되는 문제, 방장 이름이 "방장 (방장)"으로 중복 표시되는 문제, 하단 "방 삭제" 링크 대비 부족

### 확인하지 못한 것
- **실제 휴대폰 터치 드래그**(데스크톱 Chrome에서 좁은 폭으로만 확인). 코드는 프로토타입의 터치 처리(`touch-action`, 방향 판정)를 그대로 옮겼습니다.
- ~~실제 Cloudflare 배포~~ → 5절에서 배포 후 실제 주소로 확인했습니다.
- 무료 한도 초과 상황(실제로 한도를 넘길 수 없어 오류 문구 처리 코드만 작성)

## 5. 배포 (2026-09-24)

| 구분 | 주소 / 이름 |
|---|---|
| 서비스(화면) | https://deulleotdagam.jinwan.workers.dev — Workers 정적 자산 `deulleotdagam` |
| API | https://deulleotdagam-api.jinwan.workers.dev — Worker `deulleotdagam-api` + `RoomDO` |
| DB | D1 `deulleotdagam-index` (APAC) |
| 비밀값 | `IP_SALT`, `ADMIN_TOKEN` (Cloudflare에만 저장, 저장소에 없음) |

- 원래 계획은 Cloudflare Pages(`deulleotdagam.pages.dev`)였지만, Cloudflare CLI가 새 프로젝트에는 Pages 대신 Workers 정적 자산을 권장해 사용자 확인 후 그렇게 배포했습니다. 직접 도메인은 나중에 연결할 수 있습니다(README 참고).
- 배포 후 실제 주소에서 확인: 방 만들기 → 방장 ID 안내 → 외부 참여자 장식이 실시간 반영 → 규칙 검증(꼭대기에 종 거부) → ping 자동 응답 → 방 삭제 후 참여 코드 404, CORS(허용 출처만 통과), 관리자 API(토큰 없으면 401). 확인용 방은 삭제했습니다.
- 운영 Cloudflare는 가짜 `CF-Connecting-IP` 헤더가 붙은 요청을 403으로 막으므로, IP 기반 제한(방 생성·신고)을 헤더 조작으로 우회할 수 없음을 확인했습니다.

## 6. 커밋 메시지 (첫 커밋)

```
feat: 들렀다감 겨울 트리 꾸미기 서비스 첫 구현 (M1~M7)

회원가입 없이 참여 코드로 친구 방에 들러 트리에 장식과 메모를 남기는
웹 서비스를 Cloudflare 무료 플랜(Workers + Durable Objects + D1 + Pages)
위에 TypeScript 모노레포로 구현했다.

- shared: 층별 구역 기반 슬롯 생성, 별/꼭대기 규칙, 삭제 권한, 메모·이름
  검증과 금칙어 필터, 식별자(참여 코드/방장 키/게스트 토큰), 시즌, 메시지 타입
- worker: 방 생성/입장/방장 재입장/삭제 API, RoomDO(SQLite) 웹소켓 실시간
  동기화(Hibernation, rev 기반 재동기화), 30일 자동 삭제 알람, IP 해시 기반
  생성 제한, 신고 누적 비활성, 지난 시즌 전환, 관리자 API, Cron 정리
- web: 프로토타입(pixel-3trees) UI 이식, 드래그/탭/키보드 장식 달기, 조명
  꾸미기, 방장 ID 안내, 이름 바꾸기, 신고, 방 삭제, 사진(PNG) 공유와 메모
  토글, 랜덤 공개방, 지난 시즌 갤러리/읽기 전용 보기, 자동 재연결
- 테스트: 단위 27, API/웹소켓 통합 23, 정책(자동 삭제·시즌 종료·생성 제한) 7
- 미결정 사항(06-open-questions #3~#14)은 임시안으로 구현하고 TODO 표시

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
```
