# 들렀다감 — Claude Code 작업 안내

**들렀다감**(프로젝트명 `deulleotdagam`)은 회원가입 없이 링크(참여 코드)만으로 여러 명이 한 방에 들러 함께 꾸미는 웹 서비스입니다.
크리스마스 트리로 시작하고, 이후 시즌마다 꾸미는 오브젝트가 바뀝니다(봄 벚꽃나무, 여름 해변가, 가을 단풍나무).
무료 서비스이므로 **무료 호스팅 한도 안에서 동작하는 것**이 설계의 전제입니다.

## 이름 규칙

- **서비스 이름: 들렀다감** — 링크를 받고 친구 방에 들러 장식 하나 달고 메모를 남기고 가는 서비스라는 뜻. 나무, 트리 같은 오브젝트 이름을 넣지 않은 이유는 봄(벚꽃나무와 주변 사물), 여름(해변)처럼 시즌마다 꾸미는 대상이 바뀌기 때문
- **화면 표기**: 서비스 이름은 고정하고 시즌마다 부제만 바꿈. 예: "들렀다감 · 겨울 트리 꾸미기", "들렀다감 · 봄 벚꽃길", "들렀다감 · 여름 바닷가"
- **프로젝트 이름(코드용)**: `deulleotdagam`
  - 국어의 로마자 표기법 기준: 들(deul) + 렀(leot) + 다(da) + 감(gam). ㄹ과 ㄹ이 이어지면 `ll`로 적으므로 `deulleot`
  - 소문자만 사용, 단어를 나눠야 하면 하이픈(`-`)만 사용 (Cloudflare, npm, GitHub 이름 규칙 공통)
  - 코드 안의 변수/타입/파일 이름에는 서비스 이름을 넣지 않음 (예: `RoomDO`, `generateSlots`)

| 대상 | 이름 |
|---|---|
| 저장소 / 루트 폴더 | `deulleotdagam` |
| 프론트엔드 패키지 | `@deulleotdagam/web` |
| 백엔드(Worker) 패키지 | `@deulleotdagam/worker` |
| 공용 모듈 (슬롯 생성, 권한 판정, 타입) | `@deulleotdagam/shared` |
| Cloudflare Pages 프로젝트 | `deulleotdagam` (기본 주소 `deulleotdagam.pages.dev`, 이미 쓰이고 있으면 사용자에게 대안 확인) |
| Cloudflare Worker | `deulleotdagam-api` |
| D1 데이터베이스 | `deulleotdagam-index` |
| Durable Object 클래스 | `RoomDO` |
| HTML `<title>` | `들렀다감 · 겨울 트리 꾸미기` |

## 먼저 읽을 문서 (순서대로)

| 파일 | 내용 |
|---|---|
| `docs/01-product-spec.md` | 확정된 기획 사항 전체 (방, 권한, 슬롯, 삭제 정책, 모더레이션) |
| `docs/02-tech-stack.md` | 기술 스택 결정과 그 이유, 무료 한도 |
| `docs/03-architecture-and-data-model.md` | 서버 구조, 데이터 모델, API, 실시간 동기화 설계안 |
| `docs/04-tree-and-slots.md` | 나무 3그루, 층별 구역 기반 슬롯 생성 알고리즘, 조명(가랜드) 배치 규칙 |
| `docs/05-prototype-guide.md` | 프로토타입 HTML 구조와 실제 코드로 옮길 때의 기준 |
| `docs/06-open-questions.md` | 아직 결정되지 않은 항목 — 임의로 정하지 말고 사용자에게 확인할 것 |
| `docs/99-original-plan.md` | 사용자가 처음 작성한 기획 원본 (참고용) |

## 폴더 구성

```
prototype/
  pixel-3trees.html   ← 기준 프로토타입 (픽셀아트, 나무 3그루, 층별 랜덤 슬롯)
  flat-1tree.html     ← 플랫 일러스트 버전 (추후 검토용 보관, 지금은 구현 대상 아님)
assets/
  pixel/              ← 픽셀아트 SVG + twinkle-pixel.css (px- 클래스)
  flat/               ← 플랫 SVG + twinkle-flat.css (fl- 클래스) — 추후 검토용 보관
```

프로토타입은 단일 HTML 파일이라 브라우저로 바로 열립니다. 동작 확인이 필요하면 먼저 열어보세요.

## 작업 원칙

1. **언어는 TypeScript**로 통일합니다 (프론트, 백엔드, 공용 모듈 모두).
2. **아트 스타일은 픽셀아트로 먼저 진행**합니다 (기준 프로토타입 `pixel-3trees.html`, 에셋 `assets/pixel/`). 플랫 등 다른 디자인은 추후 검토 예정이므로 지금 구현하지 않되, 스타일에 의존하는 값(트리 윤곽, 좌표계, 조명 위치, 장식 목록, 반짝임 CSS)은 **테마 설정 객체로 분리**해 나중에 테마를 추가할 수 있게 만드세요.
3. 슬롯 좌표는 **방을 만들 때 서버에서 한 번 생성해 저장**합니다. 클라이언트가 시드로 다시 계산하지 않습니다 (알고리즘이 바뀌어도 기존 방 배치가 유지되어야 함).
4. 권한 판단(장식 삭제, 방장 기능)은 **반드시 서버에서** 합니다. 프로토타입의 "방장으로 보기/참여자로 보기" 토글은 테스트용이며 실제 서비스에 넣지 않습니다.
5. 무료 한도를 넘으면 요청이 **실패**합니다(느려지는 게 아님). 폴링 대신 웹소켓, 쿼리는 인덱스 사용, 방 하나를 조회할 때 전체 테이블 스캔 금지.
6. 개인정보(IP 등)는 **해시로만 저장**하고, 신고/차단/생성 횟수 제한 외 용도로 쓰지 않습니다.
7. `docs/06-open-questions.md`의 항목은 임의로 결정하지 말고 질문하세요. 구현을 막는 경우 가장 되돌리기 쉬운 쪽으로 임시 구현하고 TODO로 표시하세요.

## 추천 작업 순서 (마일스톤)

1. **프로젝트 골격**: 저장소 `deulleotdagam` 아래에 `@deulleotdagam/web`(Vite + TypeScript), `@deulleotdagam/worker`(Cloudflare Workers, wrangler), `@deulleotdagam/shared`(공용 타입/로직)를 둔 모노레포. 로컬에서 `wrangler dev`로 동작. 이름은 위 "이름 규칙" 표를 따를 것.
2. **공용 모듈 이식**: 슬롯 생성(`04` 문서), 장식 목록, 권한 판정 함수를 프로토타입에서 TS로 옮기고 단위 테스트 작성 (층별 최소 칸 보장 테스트 포함).
3. **방 생성/입장 API**: 방 생성 → 방장 키 + 참여 코드 발급, 참여 코드로 입장.
4. **꾸미기 화면**: 프로토타입 UI를 컴포넌트로 옮기고 localStorage 저장을 API/웹소켓으로 교체.
5. **실시간 동기화**: 방 Durable Object에 웹소켓 연결, 장식 추가/삭제 브로드캐스트.
6. **정책**: 자동 삭제(30일 + 5개 미만), 방 생성 횟수 제한, 금칙어, 신고 → 비활성 전환.
7. **부가 기능**: 공개방 랜덤 입장, 지난 시즌 방 둘러보기, 완성 결과 이미지 공유.

방 삭제는 **방장이 직접 "방 삭제"를 실행할 때만** 일어납니다 (그 외에는 30일 자동 삭제 규칙뿐). 3단계에서 방장 재입장과 함께 구현하세요.

각 마일스톤이 끝나면 사용자에게 확인받고 다음으로 넘어가세요.
